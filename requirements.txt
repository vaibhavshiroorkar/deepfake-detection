# Pinned versions, resolved and verified together on Python 3.13 (2026-07-04).
# CPU wheels by default; for NVIDIA GPU install see README.md (cu130 index has
# these exact torch/torchvision/torchaudio versions).
#
# Note: facenet-pytorch is pinned to 2.5.3 deliberately — 2.6.0 pins torch<2.3,
# which has no Python 3.13 wheels. 2.5.3 has unpinned deps and works with
# modern torch/numpy (verified: MTCNN runs).

torch==2.12.1
torchvision==0.27.1
torchaudio==2.11.0
numpy==2.4.6
opencv-python==5.0.0.93
librosa==0.11.0
ffmpeg-python==0.2.0
transformers==5.13.0
timm==1.0.27
facenet-pytorch==2.5.3
scikit-learn==1.9.0
pandas==3.0.3
matplotlib==3.11.0
jupyter==1.1.1
tqdm==4.68.3
