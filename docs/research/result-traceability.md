# Result traceability

## Traceability contract

Every reported number must resolve to an analysis command, content hashes, and
MLflow runs. The registry stores references. MLflow stores parameters, metrics,
runtime details, and artifacts.

## Result registry

Regenerated by `scripts/update_result_registry.py`, which declares each row next
to the command that produces it and computes the hashes from the files. Do not
edit the table by hand: a hash typed in stays put while the artifact moves, and
a stale hash still reads as evidence.

Hashes are the first 16 hex characters of the file's SHA-256. `missing` means
the artifact is not in the checkout, and that row is `pending` whatever else
resolves.


<!-- BEGIN GENERATED REGISTRY -->
| Result ID | Paper location | Analysis command | Report SHA-256 | Prediction SHA-256 | MLflow run IDs | Decision | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `A-fusion-dfdc` | Results, late fusion cross-corpus | `ddf evaluate fusion --dataset DFDC` | `258b6357cc9ea462` | `29c39dbe0773001d` | `a9ffd37c25f9` | Late fusion does not beat its own visual branch cross-corpus | provisional (1 seed) |
| `A-fusion-in-domain` | Results, late fusion in-domain | `ddf evaluate fusion --dataset FakeAVCeleb --partition test` | `5b1128f499be0804` | `a62ea676c18ae6f4` | `f31728fa29a1` | In-domain ceiling for the Design A pipeline | provisional (1 seed) |
| `A-visual-dfdc` | Results, cross-corpus baseline | `ddf evaluate branch --branch visual --dataset DFDC` | `689ec323dfb53e02` | `b30d5251103b4417` | `7f3b9f89fe93`, `1ef02d52df85` | Design A visual baseline, the number Design B is measured against | provisional (1 seed) |
| `A-visual-mnw` | Results, how generalization fails | `ddf evaluate branch --branch visual --dataset MNW` | `6745b69043a6ca28` | `98faad46c131f736` | `ec7cd9d881d8` | Detection is 25 of 85 and not uniform across generators: 0 of 10 on vasa_1 against 7 of 8 in the wild | provisional (1 seed) |
| `B-batchnorm-trade` | Results, freezing BatchNorm trades transfer for accuracy | `python scripts/batchnorm_sweep.py --run-dir runs/design-b-20260910` | missing | not applicable | none | The single largest lever measured on cross-corpus transfer. Measured in a scratch script whose output was never written under runs/. Re-run before citing. | pending |
| `B-deep-ablation` | Results, does fusing streams beat the best single stream | `python scripts/run_deep_ablation.py --run-dir runs/design-b-20260910` | `62a9f2ef9463a5fe` | `fb88596f6067316e` | none | A subset beats the full fusion cross-corpus, so more streams is not the lever | provisional (1 seed) |
| `B-fusion-ablations` | Results, late against deep and abstention against fallback | `python scripts/run_fusion_ablations.py --run-dir runs/design-b-20260910` | `7d9eec12b0356be2` | `d89e08e953338ea4` | none | The embedding carries nothing the logit does not, and abstention buys no accuracy | provisional (1 seed) |
| `B-gate-thresholds` | Method, per-media-kind decision thresholds | `python scripts/fit_gate_fusion.py --run-dir runs/design-b-20260910` | `c528bc97ba69eecc` | `fb88596f6067316e` | none | One threshold cannot serve video, image and audio | provisional (1 seed) |
| `B-stream-correlation` | Results, redundancy between streams | `python scripts/stream_correlation.py --run-dir runs/design-b-20260910` | `c0cee4a4124e51eb` | `203cd4134de01b4e` | none | Redundancy is measurable and was not where it was predicted | provisional (1 seed) |
| `B-stream-scores` | Results, per-stream ROC-AUC with intervals | `python scripts/score_streams.py --run-dir runs/design-b-20260910` | `10bf3ef7fd939d67` | `d89e08e953338ea4` | `52ba1b6cebc9`, `30247f8933e5`, `5a34ee910530`, `486fc02d6eb8` | Every stream except emotion reads near chance cross-corpus | provisional (1 seed) |
<!-- END GENERATED REGISTRY -->

## Acceptance rules

A result becomes `accepted` only when every hash resolves, every required seed
is present, and the analysis uses the frozen protocol. Failed and superseded
runs remain in MLflow. Smoke fixtures cannot enter this registry.

Three statuses are in use.

`accepted` meets every rule above. `provisional (1 seed)` has hashes that all
resolve and an analysis that follows the protocol, but was trained on one seed
where [the research design](../research-design.md) asks for three. Every Design
B result is currently in this state, and the paper reports it as a limitation
rather than rounding it up. `pending` has an artifact that is not in the
checkout; the row names the command that would produce it.

MLflow runs for the Design B streams were written by
`scripts/backfill_mlflow.py` from the history files the trainer left behind,
because Design B was trained through direct CLI calls and tracking is wired to
`ddf run --config`. Those runs carry `backfilled=true` and the SHA-256 of the
file they were built from, so a backfilled run is never mistaken for a live
one.
