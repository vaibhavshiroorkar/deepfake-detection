"""Shared, page-independent pieces of the dashboard.

Everything here is importable without a running Streamlit app unless the
docstring says otherwise, so the filtering, decoding and checkpoint logic can be
unit-tested on its own.
"""
