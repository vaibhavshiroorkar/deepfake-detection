"""Visual stream template, its config presets, and the activation tracer.

`deepfake_detection.branches` holds the frozen baseline branch that training and
evaluation use. This package holds the configurable stream the dashboard teaches
with: one class, three backbones, and hooks that report what it computed.
"""
